// todo

import { SuiGrpcClient } from "@mysten/sui/grpc";

// Get all assets
const client = SuiGrpcClient;

export function getAllAssets() {}
// Sending (Outbound Transaction)
// Receiving (Inbound Transaction)
// Swapping / Exchanging
